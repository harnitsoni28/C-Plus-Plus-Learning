#include <iostream>
using namespace std;
int main(){
    int a,b,c;
    cin >> a>> b;
    if (a==b){
        cout<<"These are equal";
    }
    else
     if(a<b){
         cout<<"a is smaller than b";
     } 
     else{
     cout<<"a is bigger than b";
     }
}